<%= @user.name %>'s profile!


<div class="reviews">
	<% @user.reviews.each do |r| %>
		<% g = Game.find(r.game_id) %>
		<h3> <%=  g.name %>  </h3>
		<p><%= @user.name %> rated this <%= r.rating %>. Their review is <%= r.content %></p>
	<% end %>
	if user = cureent user delete as an option
		<%= link_to "Delete This Review", '/games/:id/delete' %>
	<% end %>


Most Liked



Least Liked
